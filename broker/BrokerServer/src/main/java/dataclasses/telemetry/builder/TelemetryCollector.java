package dataclasses.telemetry.builder;

public enum TelemetryCollector {
    ALL,
    CPU_PRIORITY,
    CPU,
    GPU_PRIORITY,
    GPU,
    DISK_PRIORITY,
    DISK,
    NETWORK_PRIORITY,
    NETWORK
}
